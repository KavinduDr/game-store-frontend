import React from 'react'
import { GameList } from '../game'

const Context = () => {

    const products = [...Array(GameList?.length).map(() => ({
        
    }))]
  return (
    <div>Context</div>
  )
}

export default Context